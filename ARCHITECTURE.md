# MindFlow 技术架构文档

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     前端层 (Frontend)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   React UI   │  │  Zustand     │  │   Tauri API  │      │
│  │  Components  │  │    Store     │  │   Bridge     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────┬────────────────────────────────────┘
                         │ IPC (Invoke/Events)
┌────────────────────────▼────────────────────────────────────┐
│                     后端层 (Backend)                         │
│  ┌────────────────────────────────────────────────────┐    │
│  │                   Tauri v2 Framework                 │    │
│  │  ┌──────────────┐ ┌──────────────┐ ┌─────────────┐ │    │
│  │  │   Commands   │ │  Notification │ │   Events    │ │    │
│  │  │   (CRUD)     │ │    Plugin     │ │   (Remind)  │ │    │
│  │  └──────────────┘ └──────────────┘ └─────────────┘ │    │
│  └────────────────────────┬───────────────────────────┘    │
│                           │                                 │
│  ┌────────────────────────▼───────────────────────────┐    │
│  │              Rust Business Logic                    │    │
│  │  ┌──────────────┐ ┌──────────────┐ ┌─────────────┐ │    │
│  │  │   Todo Mgr   │ │  Inspire Mgr │ │   Sync Mgr  │ │    │
│  │  └──────────────┘ └──────────────┘ └─────────────┘ │    │
│  └────────────────────────┬───────────────────────────┘    │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                  数据层 (Data Layer)                         │
│  ┌────────────────────────────────────────────────────┐    │
│  │              SQLite Database                        │    │
│  │  ┌────────────────┐  ┌────────────────┐            │    │
│  │  │  todos table   │  │ inspirations   │            │    │
│  │  │  - id          │  │     table      │            │    │
│  │  │  - title       │  │  - id          │            │    │
│  │  │  - priority    │  │  - content     │            │    │
│  │  │  - due_date    │  │  - tags        │            │    │
│  │  │  - reminder_at │  │  - created_at  │            │    │
│  │  │  - completed   │  └────────────────┘            │    │
│  │  │  - archived    │                                │    │
│  │  └────────────────┘                                │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## 📦 模块说明

### 1. 前端模块

#### UI Components
- **Sidebar** - 侧边导航栏，包含统计信息和菜单
- **TodoList** - 待办列表视图，支持搜索和筛选
- **TodoItem** - 待办单项组件，支持编辑和归档
- **TodoForm** - 新建/编辑待办表单
- **InspirationList** - 灵感碎片列表
- **InspirationItem** - 灵感单项组件
- **ArchivedTodos** - 归档管理视图

#### State Management
- **Zustand Store** - 轻量级状态管理
  - 视图状态管理
  - Todo 数据缓存
  - Inspiration 数据缓存
  - 加载状态跟踪

#### API Layer
- **todoApi** - Todo 相关 Tauri 命令封装
- **inspirationApi** - Inspiration 相关 Tauri 命令封装
- **syncApi** - 数据导入导出功能

### 2. 后端模块

#### Commands (Tauri 命令)
- **todo.rs** - Todo CRUD 操作
  - `create_todo` - 创建待办
  - `get_todos` - 获取待办列表
  - `update_todo` - 更新待办
  - `complete_todo` - 完成待办（自动归档）
  - `delete_todo` - 删除待办
  - `get_todo_stats` - 获取统计信息
  - `get_todos_with_reminders` - 获取待提醒事项

- **inspiration.rs** - 灵感管理
  - `create_inspiration` - 创建灵感
  - `get_inspirations` - 获取灵感列表
  - `delete_inspiration` - 删除灵感
  - `search_inspirations` - 搜索灵感

- **sync.rs** - 同步功能
  - `get_database_path` - 获取数据库路径

#### Database Module
- **db/mod.rs** - 数据库管理
  - 数据库初始化
  - 表结构创建
  - 索引优化
  - 连接池管理

#### Models
- **Todo** - 待办数据模型
- **Inspiration** - 灵感数据模型
- **Create/Update Request** - API 请求模型

### 3. 数据同步方案

#### Local-First 架构
```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Device A   │────▶│   iCloud /   │◀────│   Device B   │
│  (Mac/iOS)   │     │   Dropbox    │     │ (Mac/iOS)    │
└──────────────┘     └──────────────┘     └──────────────┘
       │                     │                     │
       ▼                     ▼                     ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  SQLite DB   │     │  Synced DB   │     │  SQLite DB   │
└──────────────┘     └──────────────┘     └──────────────┘
```

#### 备份/恢复
- JSON 格式导出
- 完整数据恢复
- 版本兼容检查

## 🔄 数据流

### 创建 Todo 流程
```
User Input → TodoForm → todoApi.create → Tauri Invoke → 
Rust Command → SQLite Insert → Return Todo → Zustand Store → UI Update
```

### 定时提醒流程
```
Rust Background Task → Check Reminders → Fire Event → 
Tauri Notification → System Notification
```

### 完成 Todo 流程
```
User Click → todoApi.complete → Rust Command → 
Update DB (completed=true, archived=true) → 
Remove from List → Update Stats
```

## 🎨 UI 设计系统

### 配色方案
- **主色**: `#0ea5e9` (sky-500)
- **背景**: `#f9fafb` (gray-50)
- **卡片**: `#ffffff` (白色)
- **文字**: `#111827` (gray-900)

### 辅助色
- **Pastel Pink**: `#ffd1dc`
- **Pastel Blue**: `#b5d8eb`
- **Pastel Green**: `#c1e1c1`
- **Pastel Yellow**: `#fdfd96`
- **Pastel Purple**: `#e6e6fa`

### 优先级颜色
- **High**: `bg-red-100 text-red-700`
- **Medium**: `bg-yellow-100 text-yellow-700`
- **Low**: `bg-green-100 text-green-700`

### 动画效果
- **Fade In**: 0.3s ease-in-out
- **Slide Up**: 0.3s ease-out
- **Hover**: transform + shadow transition

## 🔒 安全考虑

1. **CSP (Content Security Policy)** - 配置在 tauri.conf.json
2. **Command Permissions** - 最小权限原则
3. **Database** - 应用沙盒内存储
4. **Data Export** - 用户主动触发，需要确认

## 📱 移动端适配

### 响应式断点
- **Mobile**: < 768px - 侧边栏抽屉式
- **Tablet**: 768px - 1024px - 紧凑布局
- **Desktop**: > 1024px - 完整布局

### 触摸优化
- 按钮最小 44px 点击区域
- 滑动手势支持
- 软键盘适配

## 🚀 性能优化

1. **数据库索引** - 已添加常用查询字段索引
2. **虚拟滚动** - 大量数据时启用（预留）
3. **懒加载** - 图片和重型组件懒加载（预留）
4. **Memoization** - React.memo 优化重渲染
5. **Debouncing** - 搜索输入防抖

## 🔧 扩展性

### 插件系统
- 预留了插件接口
- 可扩展通知、同步等功能

### 主题定制
- Tailwind CSS 配置支持深色模式
- 预留主题切换功能

## 📊 监控与调试

### 日志
- Rust: `log` crate
- 前端: `console.log`（生产环境移除）

### 错误处理
- 全局错误边界
- 命令错误捕获
- 用户友好的错误提示

## 📝 开发规范

### 代码风格
- Rust: `cargo fmt` + `cargo clippy`
- TypeScript: 严格模式
- React: 函数组件 + Hooks

### Git 工作流
- `main` - 稳定分支
- `develop` - 开发分支
- `feature/*` - 功能分支

---

*文档版本: 1.0*
*最后更新: 2025-01-30*
